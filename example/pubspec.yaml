name: example
description: An example Flutter project for adblocker webview
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.19.5'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.5
  adblocker_webview:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
